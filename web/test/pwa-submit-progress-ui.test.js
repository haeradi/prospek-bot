'use strict';
const test=require('node:test'),assert=require('node:assert/strict'),fs=require('fs'),path=require('path');
const root=path.join(__dirname,'..'),html=fs.readFileSync(path.join(root,'public/index.html'),'utf8'),js=fs.readFileSync(path.join(root,'public/app.js'),'utf8'),css=fs.readFileSync(path.join(root,'public/styles.css'),'utf8');
test('submit ASSIST mengunci tombol dan menampilkan progress plus ETA',()=>{assert.match(js,/submitAllConfirm\.disabled=true/);assert.match(js,/submitAllProgress/);assert.match(js,/Perkiraan sisa/);assert.match(html,/id="submitAllProgress"/)});
test('portal installable sebagai PWA',()=>{const server=fs.readFileSync(path.join(root,'src/app.js'),'utf8');assert.match(html,/rel="manifest" href="\/manifest\.webmanifest/);assert.match(js,/serviceWorker\.register\(['"]\/sw\.js/);assert.match(server,/manifest\.webmanifest/);assert.match(server,/icon\.svg/);assert.equal(fs.existsSync(path.join(root,'public/manifest.webmanifest')),true);assert.equal(fs.existsSync(path.join(root,'public/sw.js')),true)});
test('layout responsif mobile tablet desktop',()=>{assert.match(html,/width=device-width, initial-scale=1/);assert.match(css,/@media\(max-width:900px\)/);assert.match(css,/@media\(max-width:600px\)/);assert.match(css,/overflow-x:auto/)});
