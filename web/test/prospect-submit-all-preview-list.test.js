'use strict';
const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');
const {DatabaseSync}=require('node:sqlite');
const {installProspectSubmitAllService}=require('../src/prospect-submit-all-service');
test('preview Kirim Semua membawa detail Daftar Lokal milik Sales',async()=>{const db=new DatabaseSync(':memory:');db.exec(`CREATE TABLE users(id TEXT PRIMARY KEY,role TEXT,status TEXT);CREATE TABLE prospects(id TEXT PRIMARY KEY,owner_id TEXT,status TEXT,created_at TEXT,level TEXT,customer_name TEXT,phone TEXT,motor TEXT,input_origin TEXT);INSERT INTO users VALUES('u1','SALES','ACTIVE');INSERT INTO prospects VALUES('p1','u1','DRAFT','1','HOT','Ani','0812','Vario','EXCEL');INSERT INTO prospects VALUES('p2','u1','DRAFT','2','LOW','Manual','0813',NULL,'MANUAL');`);const service=installProspectSubmitAllService({db,submitService:{}}),op=await service.preview('u1','list-key-123');assert.deepEqual(op.items,[{prospectId:'p1',ordinal:0,operationId:null,status:'PENDING',errorCode:null,customerName:'Ani',phone:'0812',level:'HOT',motor:'Vario'}])});
test('UI preview Kirim Semua menyediakan dan merender Daftar Lokal dengan textContent',()=>{const html=fs.readFileSync(path.join(__dirname,'../public/index.html'),'utf8'),js=fs.readFileSync(path.join(__dirname,'../public/app.js'),'utf8');assert.match(html,/id="submitAllRows"/);assert.match(js,/submitAllRows/);assert.match(js,/customerName/);assert.match(js,/textContent/)});
test('UI menyembunyikan daftar Excel utama saat review Kirim Semua tampil dan memulihkannya saat batal',()=>{const html=fs.readFileSync(path.join(__dirname,'../public/index.html'),'utf8'),js=fs.readFileSync(path.join(__dirname,'../public/app.js'),'utf8');assert.match(html,/id="excelProspectSection"/);assert.match(js,/excelProspectSection'\)\.hidden=true/);assert.match(js,/excelProspectSection'\)\.hidden=false/)});
